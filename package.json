{
  "name": "thoughtdrop",
  "version": "1.0.1",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "NODE_ENV=development node --experimental-vm-modules api/send-quote.js",
    "init-db": "node -e \"import('./api/init-db.js').then(m => m.default({query:{}}, {status:()=>({json:console.log})}))\"",
    "start": "vercel dev"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "dotenv": "^16.4.5",
    "nodemailer": "^8.0.7",
    "openai": "^4.28.0"
  }
}
